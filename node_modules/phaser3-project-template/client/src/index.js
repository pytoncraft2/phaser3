// import Personnage from './scenes/Personnage';
// import Chambre from './scenes/Chambre';
var config = {
  type: Phaser.AUTO,
  width: 1500,
  height: 720,
  scene: [Personnage, Chambre]
};

var game = new Phaser.Game(config);
console.log("heyllo");
var socket = io('http://localhost:3000');

    socket.on('smaller', function(taille) {
      console.log(taille);
      // var player = ig.game.getEntitiesByType(EntityPlayer)[0];
      // if (player) {
        // player.messagebox = player.messagebox + '\n' + data + ' disconnected';
      // }
    });
