import io from 'socket.io-client';
import Phaser from "phaser";
// import Chambre from "./src/scenes/Chambre";
// import Personnage from "./src/scenes/Personnage";


var config = {
  type: Phaser.AUTO,
  width: 1500,
  height: 720
  // scene: [Personnage, Chambre]
};

var game = new Phaser.Game(config);
